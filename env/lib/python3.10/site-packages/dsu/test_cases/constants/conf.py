from jinja2 import Environment, PackageLoader

env = Environment(
    loader=PackageLoader(
        'dsu.test_cases',
        'templates'
    ),
    autoescape=False,
    trim_blocks=False
)


def render_test_cases(name, context):
    return env.get_template(name).render(context)
