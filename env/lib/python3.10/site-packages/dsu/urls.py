# -*- coding: utf-8 -*-
from django.urls import re_path

from dsu import views

app_name = 'dsu'

urlpatterns = [
    re_path(r'^chartit/', views.chartit, name='chartit'),
    re_path(r'^(?P<path>.*)/?$', views.swagger_ui, name='swagger_ui'),
]
