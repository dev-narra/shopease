"""
Successful case
"""
import mock

from ib_users.utils.custom_test_utils import CustomTestUtils, generate_uuid
from . import APP_NAME, OPERATION_NAME, REQUEST_METHOD, URL_SUFFIX

REQUEST_BODY = """
{
    "new_username": "username019"
}
"""

TEST_CASE = {
    "request": {
        "path_params": {},
        "query_params": {},
        "header_params": {},
        "securities": {"oauth": {"tokenUrl": "http://www.example.com",
                                 "flow": "password",
                                 "scopes": ["read", "write"],
                                 "type": "oauth2"}},
        "body": REQUEST_BODY,
    },
}


class TestCase01ChangeUsernameAPITestCase(CustomTestUtils):
    app_name = APP_NAME
    operation_name = OPERATION_NAME
    request_method = REQUEST_METHOD
    url_suffix = URL_SUFFIX
    test_case_dict = TEST_CASE

    @mock.patch('uuid.uuid4', generate_uuid)
    def setUp(self):
        user_account_objs = self.populate_user_account()
        self.foo_user = user_account_objs[0]

    def _create_user(self, username, password, is_staff=False):
        return self.foo_user

    def test_case(self):
        self.default_test_case()
