"""
# TODO: Update test case description
"""

from django_swagger_utils.utils.test import CustomAPITestCase

from . import APP_NAME, OPERATION_NAME, REQUEST_METHOD, URL_SUFFIX

REQUEST_BODY = """
{
    "name": "string",
    "gender": "MALE",
    "dob": "2099-12-31",
    "profile_pic_url": "string",
    "phone_number": "9988776655",
    "email": "username@user.com",
    "country_code": "91",
    "language_preference": "ENGLISH"
}
"""

TEST_CASE = {
    "request": {
        "path_params": {},
        "query_params": {},
        "header_params": {},
        "securities": {"oauth": {"tokenUrl": "http://www.example.com",
                                 "flow": "password", "scopes": ["write"],
                                 "type": "oauth2"}},
        "body": REQUEST_BODY,
    },
}


class TestCase02CreateUserProfileAPITestCase(CustomAPITestCase):
    app_name = APP_NAME
    operation_name = OPERATION_NAME
    request_method = REQUEST_METHOD
    url_suffix = URL_SUFFIX
    test_case_dict = TEST_CASE

    def setupUser(self, username, password):

        super(TestCase02CreateUserProfileAPITestCase, self).setupUser(
            username=username,
            password=password
        )

        from ib_users.models import UserProfile
        UserProfile.objects.create(
            account=self.foo_user,
            name="name",
            email=self.foo_user.email
        )


    def test_case(self):
        self.default_test_case()  # Returns response object.
        # Which can be used for further response object checks.
        # Add database state checks here.
