def generate_new_referral_code():
    import string
    import random
    from ib_users.constants.referral_constants import REFERRAL_CODE_LENGTH
    referral_code = ''.join(random.choices(
        string.ascii_uppercase + string.digits, k=REFERRAL_CODE_LENGTH))
    return referral_code
