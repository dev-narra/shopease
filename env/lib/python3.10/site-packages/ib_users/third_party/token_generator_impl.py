from django.contrib.auth.models import AbstractUser
from django.contrib.auth.tokens import PasswordResetTokenGenerator

from ib_users.interactors.third_party.token_generator import TokenGenerator


class TokenGeneratorImpl(TokenGenerator, PasswordResetTokenGenerator):
    def get_token(self, user: AbstractUser) -> str:
        return super().make_token(user)