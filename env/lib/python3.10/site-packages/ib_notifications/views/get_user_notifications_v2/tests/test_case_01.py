"""
# TODO: Update test case description
"""
import freezegun
import pytest
from django_swagger_utils.utils.test import CustomAPITestCase
from . import APP_NAME, OPERATION_NAME, REQUEST_METHOD, URL_SUFFIX
from ib_notifications.tests.factories import models as model_factories


REQUEST_BODY = """

"""

TEST_CASE = {
    "request": {
        "path_params": {},
        "query_params": {
            "limit": 297,
            "offset": 91,
            "notifications_category": "INVALID_NOTIFICATION_CATEGORY"
        },
        "header_params": {},
        "securities": {
            "oauth": {
                "tokenUrl": "http://www.example.com",
                "flow": "password",
                "scopes": ["read"],
                "type": "oauth2"
            }
        },
        "body": REQUEST_BODY,
    },
}


@pytest.mark.django_db
class TestCase01GetUserNotificationsV2APITestCase(CustomAPITestCase):
    app_name = APP_NAME
    operation_name = OPERATION_NAME
    request_method = REQUEST_METHOD
    url_suffix = URL_SUFFIX
    test_case_dict = TEST_CASE

    @freezegun.freeze_time("2020-09")
    def test_case(self):
        self.default_test_case()
