from django.apps import AppConfig


class IbNotificationsAppConfig(AppConfig):
    name = "ib_notifications"

    def ready(self):
        from ib_notifications import signals # pylint: disable=unused-variable
