"""
# TODO: Update test case description
"""

from django_swagger_utils.utils.test import CustomAPITestCase
from mock import mock
from django.test import override_settings

from s3_uploader.utils.test_utils.boto3_patches.abstract_boto3_service import \
    boto3_service
from . import APP_NAME, OPERATION_NAME, REQUEST_METHOD, URL_SUFFIX

REQUEST_BODY = """

"""

TEST_CASE = {
    "request": {
        "path_params": {},
        "query_params": {},
        "header_params": {},
        "securities": {"oauth": {"scopes": ["read", "write"],
                                 "tokenUrl": "http://www.example.com",
                                 "flow": "password", "type": "oauth2"}},
        "body": REQUEST_BODY,
    },
}


class TestCase01GetS3CredentialsAPITestCase(CustomAPITestCase):
    app_name = APP_NAME
    operation_name = OPERATION_NAME
    request_method = REQUEST_METHOD
    url_suffix = URL_SUFFIX
    test_case_dict = TEST_CASE

    @mock.patch('boto3.client', boto3_service)
    @override_settings(MEDIAFILES_LOCATION='')
    @override_settings(DEFAULT_CLOUDFRONT_PROTOCOL='https')
    @override_settings(AWS_STORAGE_BUCKET_NAME='bucket-name')
    @override_settings(AWS_CLOUDFRONT_DOMAIN='cf.aws.com')
    @override_settings(AWS_S3_REGION_NAME='ap-south-1')
    def test_case(self):
        self.default_test_case()  # Returns response object.
        # Which can be used for further response object checks.
        # Add database state checks here.
