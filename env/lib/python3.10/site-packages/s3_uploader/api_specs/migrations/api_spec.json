{
  "swagger": "2.0",
  "host": "127.0.0.1:8000",
  "basePath": "/api/s3_uploader/",
  "info": {
    "version": "1.0.0",
    "title": "Simple API",
    "description": "A simple API to learn how to write OpenAPI Specification"
  },
  "schemes": [
    "http"
  ],
  "consumes": [
    "application/json"
  ],
  "produces": [
    "application/json"
  ],
  "securityDefinitions": {
    "oauth": {
      "tokenUrl": "http://auth.example.com/oauth2/",
      "flow": "password",
      "scopes": {
        "read": "read users",
        "write": "create users",
        "update": "update users",
        "delete": "delete users"
      },
      "type": "oauth2"
    }
  },
  "security": [
    {
      "oauth": [
        "read",
        "write"
      ]
    }
  ],
  "definitions": {
    "S3Credentials": {
      "type": "object",
      "properties": {
        "aws_access_key_id": {
          "type": "string"
        },
        "aws_session_token": {
          "type": "string"
        },
        "bucket_name": {
          "type": "string"
        },
        "cloudfront_domain": {
          "type": "string"
        },
        "expiration": {
          "type": "string"
        },
        "folder_name": {
          "type": "string"
        },
        "region_name": {
          "type": "string"
        },
        "secret_access_key": {
          "type": "string"
        },
        "protocol": {
          "type": "string",
          "enum": [
            "http",
            "https"
          ]
        }
      },
      "required": [
        "aws_access_key_id",
        "aws_session_token",
        "secret_access_key",
        "region_name",
        "expiration",
        "protocol"
      ]
    }
  },
  "parameters": {
  },
  "responses": {
    "SuccessResponse": {
      "description": "success response"
    },
    "GetS3Credentials": {
      "description": "returns user's s3 credentials",
      "schema": {
        "$ref": "#/definitions/S3Credentials"
      }
    }
  },
  "paths": {
    "/credentials/v1/": {
      "get": {
        "summary": "get user's s3 credentials",
        "description": "get user's s3 credentials",
        "operationId": "get_s3_credentials",
        "responses": {
          "200": {
            "$ref": "#/responses/GetS3Credentials"
          }
        },
        "tags": [
          "v1"
        ]
      }
    }
  }
}
    