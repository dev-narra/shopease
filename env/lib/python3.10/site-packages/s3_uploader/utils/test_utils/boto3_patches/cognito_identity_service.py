# pylint: disable-all

from datetime import datetime


class CognitoIdentityService(object):

    def get_open_id_token_for_developer_identity(self, **kwargs):
        return {
            'IdentityId': 'string',
            'Token': 'string'
        }

    def get_credentials_for_identity(self, **kwargs):
        return {
            'IdentityId': 'string',
            'Credentials': {
                'AccessKeyId': 'string',
                'SecretKey': 'string',
                'SessionToken': 'string',
                'Expiration': datetime(2015, 1, 1)
            }
        }
